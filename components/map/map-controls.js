import React from 'react';
import PropTypes from 'prop-types';

// Components

export default function MapControls(props) {
  return (
    <aside className="c-map-controls">
    </aside>
  );
}

MapControls.propTypes = {
};
