
const MAP_OPTIONS = {
  zoom: 8,
  minZoom: 2,
  maxZoom: 8,
  center: [30, -120],
  zoomControl: true,
  detectRetina: true
};

const GENERIC_ZINDEX = 500;

export {
  MAP_OPTIONS,
  GENERIC_ZINDEX
};
